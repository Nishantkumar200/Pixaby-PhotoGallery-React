import { makeStyles } from "@material-ui/core/styles";

export default makeStyles({
  heading: {
    marginTop:30,
    fontWeight: 600,
    textAlign: "center",
    color: "#2B0425",
  },
  subtitle: {
    textAlign: "center",
    marginTop: 15,
    color:"blue"
  },
});
